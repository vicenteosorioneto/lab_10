# ============================================================
# Lab 10 — LLM Optimization Benchmark
# Requirements
# ============================================================

# --- Deep Learning Core ---
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# --- Transformers Ecosystem ---
transformers>=4.40.0
accelerate>=0.29.0
tokenizers>=0.19.0
safetensors>=0.4.3
huggingface-hub>=0.23.0

# --- QLoRA / Quantização ---
bitsandbytes>=0.43.0
peft>=0.10.0

# --- FlashAttention-2 (GPU Ampere+ necessária, opcional) ---
# Instale manualmente se tiver GPU compatível (compute capability >= 8.0):
# pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# --- Análise Numérica e Dados ---
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0

# --- Visualização ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Utilitários ---
tqdm>=4.66.0
psutil>=5.9.0
rich>=13.7.0

# --- Desenvolvimento / Reprodutibilidade ---
ipython>=8.23.0
jupyter>=1.0.0
notebook>=7.1.0
